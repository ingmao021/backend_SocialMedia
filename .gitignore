# ── Build artifacts ──
target/
*.class

# ── IDE ──
.idea/
*.iml
.vscode/
*.swp
*.swo

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── Environment ──
.env
.env.local
.env.*.local

# ── Secrets — CRÍTICO ──
service-account-key.json
*-credentials.json
*.pem
*.key
gcp-key*.json

# ── Maven wrapper ──
# NO descomentar las siguientes líneas — .mvn/, mvnw y mvnw.cmd
# DEBEN estar versionados para que cualquier dev pueda buildear sin
# tener Maven instalado.

# ── Logs ──
*.log
logs/